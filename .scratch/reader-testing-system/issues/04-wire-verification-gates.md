# Wire Verification Gates

Status: ready-for-agent

## Goal

Make the new test system easy to run alongside existing quality checks.

## Acceptance Criteria

- Frontend tests pass with `pnpm test:run`.
- Frontend lint/type checks still pass with `pnpm check`.
- Backend tests pass with `cd src-tauri && cargo test`.
- Documentation names the commands maintainers should run.

## Comments
