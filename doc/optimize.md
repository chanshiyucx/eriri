# 电子书阅读优化

## 按章节懒加载

我的每本电子书大概 2-10 万字，有多个章节，直接阅读可能会导致卡顿，建议实现按章节懒加载的机制，同时按章节跳转和继续阅读的功能不受影响。

### 实现方案

- 只渲染当前章节
- 切换章节时才加载新内容
- 可正常跳转到其他章节和继续阅读

## 多标签页优化

### 问题

- 多标签页累积内存过大
- 不活跃标签页占用资源浪费
- 标签页切换时可能卡顿

### 实现方案

- 只渲染当前最新访问的5个标签页的内容
- 其余非活跃标签页只保留元数据
- 切换到非活跃标签页时，从元数据恢复内容和进度
