# 漫画阅读器重构

当前组件支持单页和双页模式。我计划将双页模式改为滚动模式。

- 单页模式保持当前逻辑一致，一屏只显示一张图片
- 双页模式改为滚动模式，滚动模式可以参考 ComicLibrary 组件的第二栏 scroll 模式，只不过是改为横向滚动。具体细节如下：
  - 图片高度撑满，宽度自适应，保持原比例
  - 图片横向排列，可以横行滚动，不需要左右箭头
  - 参考 ComicLibrary 使用虚拟 dom，同时使用略缩图优化加载，可以考虑复用 ScrollImageItem 组件。
